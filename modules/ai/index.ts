/**
 * Optional, bring-your-own-key AI layer: provider adapters (OpenAI,
 * Gemini, Claude) behind a single `generateStructured` interface,
 * AI Audit + Opportunity Reasoning only (architecture.md §11).
 *
 * Not implemented yet — scoped for Sprint 5 (AI Intelligence).
 */
export {};
